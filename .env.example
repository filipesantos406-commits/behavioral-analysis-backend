# Variáveis de Ambiente — Behavioral Analysis Backend

# ─── OBRIGATÓRIO ─────────────────────────────────────────────
# Chave de API do Google Gemini
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=sua_chave_aqui

# ─── OPCIONAL ─────────────────────────────────────────────────
# Porta do servidor (padrão: 3000)
PORT=3000

# Modelo Gemini a usar (padrão: gemini-1.5-flash)
GEMINI_MODEL=gemini-1.5-flash

# Timeout para chamadas Gemini em ms (padrão: 30000)
GEMINI_TIMEOUT_MS=30000

# Limite máximo de caracteres aceitos no campo "mensagem" (padrão: 10000)
MAX_MESSAGE_LENGTH=10000

# Versão do motor analítico (aparece na resposta JSON)
ENGINE_VERSION=1.0.0
